# BenimBazar — local / production secrets (copy to .env, never commit)

# Google Sign-In (Web OAuth 2.0 client)
GOOGLE_CLIENT_ID=
GOOGLE_WEB_CLIENT_ID=

# Optional CORS (comma-separated; default *)
# BENIMBAZAR_CORS_ORIGINS=http://localhost,http://changex.mehmetfer.com.tr
