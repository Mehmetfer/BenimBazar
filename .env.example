# CHANGE X — local / production secrets (copy to .env, never commit)

# Google Sign-In (Web OAuth 2.0 client — same value in Flutter dart-define)
GOOGLE_CLIENT_ID=
GOOGLE_WEB_CLIENT_ID=

# Optional CORS (comma-separated; default *)
# CHANGE_X_CORS_ORIGINS=http://localhost,http://changex.mehmetfer.com.tr
