name: changex
description: CHANGE X — para yok, sadece takas (Mandal / Dirhem / Madalyon).
publish_to: 'none'
version: 0.1.4+8

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.3.2
  shared_preferences: ^2.5.3
  crypto: ^3.0.7
  http: ^1.2.2
  image_picker: ^1.1.2
  http_parser: ^4.0.2
  file_picker: ^11.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
